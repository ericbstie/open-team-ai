//! Placeholder crate root — the `openteam-wire` contract crate is implemented in
//! the next build phase (see docs/adr/0013, 0018, 0012, 0008). Present only so the
//! virtual workspace parses; the wire types, identity grammar, `Seed`, and
//! `TokenCounter` land here.
